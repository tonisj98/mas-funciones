// Esta función debería devolver el cuadrado de un número, pero no lo hace. Corrige el error

function squaed(b) {
    b = b *  b
    return b
}

console.log(squaed(3)); // 9
console.log(squaed(5)); // 25
